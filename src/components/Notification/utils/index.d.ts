import { ENotificationType } from "../..";
export declare const showNotification: ({ iconType, title, duration, description, }: {
    iconType: ENotificationType;
    title: string;
    duration: number;
    description: string;
}) => void;
